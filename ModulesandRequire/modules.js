// const fighters = require('./kenganAshura');
const { fighter, ages } = require('./kenganAshura');

console.log(fighter);
console.log(ages)
//console.log(fighters.a);
// console.log(fighter); // just because we import files using require doesn't mean that we can access the variables from the file.