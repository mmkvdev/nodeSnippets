/*
    - printing out directories and files in nodejs.
    - directories -> __dirname -> returns absolute path of the working directory
    - files -> __filename -> returns the absolute path of the working directory along with the specified filename.
*/

console.log('directory', __dirname);
console.log('filename', __filename);